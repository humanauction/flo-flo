[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --disable-warnings --maxfail=1
markers =
    external: tests that require internet or third-party services
    openai: tests that require OPENAI_API_KEY and paid quota